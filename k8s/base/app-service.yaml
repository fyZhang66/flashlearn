apiVersion: v1
kind: Service
metadata:
  name: app
  namespace: flashlearn
  labels:
    app: flashlearn
spec:
  selector:
    app: flashlearn
  ports:
    - name: http
      port: 80
      targetPort: http
